# 开发日志 · 作品集首页（index.html）

**维护**：变更 hero、项目卡、图表、TOC、主题开关或 Neu 段时更新本文件。

---

## 记录

### 2026-05-11

- **迭代史**：各「📜 查看完整迭代历史」去掉港理工/在读/课业并行表述；时间收束到 **2026.05**（与简历一致）；内嵌创作舱 dev-flow `aria-label` 改为「2025 秋起」。
- **Dev 日志**：上述迭代列表已镜像到各 Demo 的 `dev-logs/*.md` →「作品集迭代记录」节，便于单机复盘。
- **UI**：新拟物（Neu）与字体仍在 `index.html` 内联；各子 Demo 已外挂 `neumorphic-shared.css` 统一 token。主站若改阴影/圆角，建议同步改 `neumorphic-shared.css` 再对照本页内联是否需合并。

### 2026-05-11（基线）

- 主站作为叙事入口；`data-ui` 未挂载在本文件，避免与内联大段样式冲突。
